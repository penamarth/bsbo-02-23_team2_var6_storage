![alt text](../assets/find.png)

```
@startuml
actor "Кладовщик" as Clerk
actor "Менеджер склада" as Manager
participant "WarehouseController" as Controller
participant "WarehouseFacade" as Facade
participant "StorageLocationAssignmentUseCase" as UseCase
participant "LocationAssignmentContext" as Context
participant "LocationAssignmentService" as Strategy
participant "LocationRepository" as Repo

Clerk -> Controller: assignLocation(AssignmentCommand)
Manager -> Controller: assignLocation(AssignmentCommand)

Controller -> Facade: assignLocation(command)
Facade -> UseCase: assignLocation(command)
UseCase -> Context: executeStrategy(dimensions, quantity)
Context -> Strategy: findOptimalLocation(dimensions, quantity)
Strategy -> Repo: findAvailableLocations(dimensions, quantity)
Repo --> Strategy: List<StorageLocation>
Strategy --> Context: LocationId
Context --> UseCase: LocationId
UseCase --> Facade: Result<LocationId>
Facade --> Controller: Result<LocationId>
Controller --> Clerk: LocationId
Controller --> Manager: LocationId
@enduml
```
